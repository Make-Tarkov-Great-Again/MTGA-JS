
class AccountServer {

    constructor() {
        this.accounts = {};
        this.accountsFileAge
    }

    /**
      * reloadAccountByLogin functions checks for changes in 
      * profile account data on user login and loads accounts on demand.
      * @param {object} info 
      * @returns user account ID
      */
    reloadAccountByLogin(info) {

    }
}